<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sleekydz86.service.commu.mapper.NotificationMapper">

    <insert id="insertNotification" parameterType="com.sleekydz86.service.commu.entity.Notification" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO notifications (
            user_id, message, type, timestamp
        ) VALUES (
            #{userId}, #{message}, #{type}, #{timestamp}
        )
    </insert>

    <select id="findByUserIdOrderByTimestampDesc" parameterType="long" resultType="com.sleekydz86.service.commu.entity.Notification">
        SELECT
            id,
            user_id as userId,
            message,
            type,
            timestamp
        FROM notifications
        WHERE user_id = #{userId}
        ORDER BY timestamp DESC
    </select>

</mapper>

